apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: storage-critical
value: 1000000000
description: "Critical storage controllers required for persistent volume management and recovery."
preemptionPolicy: PreemptLowerPriority