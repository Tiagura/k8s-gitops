apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cloudflared
commonLabels:
  app.kubernetes.io/name: cloudflared
  app.kubernetes.io/managed-by: argocd

resources:
  - cloudflared-tunnel-token-secret-sealed.yaml
  - config-map.yaml
  - deployment.yaml
  - namespace.yaml