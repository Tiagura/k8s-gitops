apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - class.yaml
  - namespace.yaml
  - gateway-l2-announce.yaml
  - gateway-internal.yaml

generatorOptions:
  disableNameSuffixHash: true