apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: gitops-critical
value: 900000
description: "Critical GitOps controllers required for cluster reconciliation"
preemptionPolicy: PreemptLowerPriority